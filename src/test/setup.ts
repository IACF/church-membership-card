// Ponto de setup global dos testes (jest-expo).
// Os matchers do Jest da React Native Testing Library (ex.: toBeOnTheScreen)
// já vêm embutidos e são registrados automaticamente — não é preciso importar.
// Configuração adicional (ex.: server MSW) entra aqui nas próximas fases.
export {};
